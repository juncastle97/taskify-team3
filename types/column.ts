export interface ColumnDataType {
  result: string;
  data: [
    {
      id: number;
      title: string;
      teamId: string;
      createdAt: string;
      updatedAt: string;
    },
  ];
}
export interface DataType {
  title: string;
}
